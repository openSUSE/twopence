sudo: required
dist: trusty
language: c
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libssh-dev ruby-dev rake-compiler python-dev make pkg-config
script:
  - make
